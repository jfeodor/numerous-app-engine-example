if [[ ! -f ~/.keyring-env.sh ]]; then
    DBUS_ENV=$(dbus-launch --sh-syntax)
    KEYRING_ENV=$(echo "" | gnome-keyring-daemon --unlock)
    echo "$DBUS_ENV" >> .keyring-env.sh
    echo "$KEYRING_ENV" >> .keyring-env.sh
fi

eval $(cat .keyring-env.sh)