
# fix numerous login
if [[ ${DBUS_SESSION_BUS_ADDRESS} = "" ]]; then
    # if no dbus session is active, start one
    exec dbus-run-session -- bash
else
    # if a dbus session is active, unlock the keyring
    echo -n "" | gnome-keyring-daemon --unlock > /dev/null
fi
